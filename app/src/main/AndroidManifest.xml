<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="cn.yanglj65.www.ecloth_app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignupActivity" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:launchMode="singleTop" />
        <activity
            android:name=".AddActivity"
            android:label="@string/title_activity_add"
            android:theme="@style/AppTheme" />
        <activity android:name=".PersonInfoActivity" />
        <activity
            android:name=".SettingActivity"
            android:parentActivityName=".HomeActivity" />
        <activity android:name=".BugReportActivity" />
        <activity android:name=".CommonSettingsActivity" />
        <activity android:name=".RemindActivity" />
        <activity android:name=".PrivacyActivity" />
        <activity android:name=".ModifyPwdActivity" />
        <activity android:name=".ClothDetailActivity" />
        <activity android:name=".OccasionActivity" />
        <activity android:name=".RandomResultActivity" />
    </application>

</manifest>